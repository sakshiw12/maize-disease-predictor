# 🌽 Maize Disease Prediction (Flask + ML)

This project predicts **maize crop diseases** using **crop growth stage** with a Random Forest model.  
The app is deployed using **Render** and built using:

- Flask (backend web app)
- scikit-learn (machine learning)
- pandas, numpy (data handling)
- HTML/CSS (UI)

---

## 📁 Project Structure

